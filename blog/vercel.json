{
    "buildCommand": "pip install --break-system-packages -r requirements.txt && python manage.py collectstatic --noinput && mkdir -p public",
    "installCommand": "pip install --break-system-packages -r requirements.txt",
    "outputDirectory": "public",
    "routes": [
        {
            "src": "\/(.*)",
            "dest": "api/index.py"
        }
    ]
}